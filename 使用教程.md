# AI 答题助手 — 使用教程

## 一、准备工作（只需做一次）

### 1. 安装 Python

- 下载：https://www.python.org/downloads/
- 安装时 **勾选 "Add Python to PATH"**（重要！）
- 验证安装：按 `Win+R`，输入 `cmd`，输入 `python --version`，看到版本号即成功

### 2. 获取 DeepSeek API Key

- 打开：https://platform.deepseek.com/api_keys
- 注册账号 → 点击「创建 API Key」→ 复制保存（格式：`sk-xxxxxxxx`）
- 新用户有免费额度

### 3. 设置 API Key（二选一）

**方法 A（推荐）：创建 `.env` 文件**

在项目文件夹 `QSanswer` 下新建一个 `.env` 文件，写入：

```
DEEPSEEK_API_KEY=sk-你的API-Key
```

**方法 B：设置系统环境变量**

按 `Win+R`，输入 `cmd`，执行：

```
setx DEEPSEEK_API_KEY "sk-你的API-Key"
```

（设置后需重新打开命令行窗口或重启电脑生效）

### 4. 安装 Tampermonkey 扩展

- **Firefox**：https://addons.mozilla.org/firefox/addon/tampermonkey/
- **Edge**：https://microsoftedge.microsoft.com/addons/detail/tampermonkey/iidnbdihhbdjgggkpfinihbpcmpjbjma

安装后浏览器右上角会出现 🐵 Tampermonkey 图标。

---

## 二、启动服务（每次使用前）

1. 双击项目文件夹中的 **`start.bat`**
2. 看到 "服务运行中" 字样表示启动成功
3. **不要关闭这个命令行窗口**，它需要一直在后台运行

> 如果没有 API Key，启动时会提示你输入。你也可以先创建 `.env` 文件再启动。

---

## 三、安装用户脚本（只需做一次）

1. 用记事本打开项目文件夹中的 **`tampermonkey_script.js`** 文件
2. **全选 复制** 全部内容（`Ctrl+A` → `Ctrl+C`）
3. 点击浏览器右上角 **Tampermonkey 图标** → 选择「管理面板」
4. 点击左上角 **`+` 新建脚本**
5. 清空编辑器中的默认内容 → **粘贴** 刚才复制的脚本 → `Ctrl+S` 保存
6. 确认脚本左侧开关为 **绿色（已启用）**

---

## 四、开始使用

### 打开面板

1. 打开任意网页（比如刷题网站）
2. 页面右下角会出现一个 **紫色悬浮球 📝**
3. **点击悬浮球** → 展开答题面板

### 截图模式（推荐）

这是最常用的模式：

1. 确认面板上方选中「📸 截图答题」
2. 用系统截图工具截取题目：按 **`Win + Shift + S`**，框选题目区域
3. 回到浏览器，点击面板中的截图区域，按 **`Ctrl + V`** 粘贴截图
4. 选择推理模式（普通 / 深度思考 / 极限推理）
5. 点击「🚀 开始答题」
6. 答案会在下方显示

### 自动检测模式

适合页面文字可以直接选中的情况：

1. 切换到「🔍 自动检测」选项卡
2. 选择推理模式
3. 点击「🚀 开始答题」
4. 脚本会自动抓取页面文字，检测题目并解答

### 使用答案

- **📋 复制答案**：一键复制到剪贴板
- **✏️ 填入页面**：自动填入当前光标所在的输入框，或页面上第一个输入框

---

## 五、推理模式说明

| 模式 | 速度 | 适用场景 |
|------|------|---------|
| ⚡ 普通 | 快（2-5秒） | 简单选择题、判断题 |
| 🧠 深度思考 | 中等（10-30秒） | 阅读理解、中等计算题 |
| 🔥 极限推理 | 慢（30-90秒） | 数学证明、复杂推理题 |

> 如果答案不准确，试试切换到更高的推理模式。

---

## 六、常见问题

### Q: 点「开始答题」显示「连接失败」？

**A:** Python 服务没有启动。双击 `start.bat` 启动服务，确认命令行窗口没有关闭。

### Q: 看不到悬浮球？

**A:** 
1. 检查 Tampermonkey 脚本是否已启用（去管理面板确认开关是绿色）
2. 刷新页面（`F5`）
3. 检查 Tampermonkey 图标上是否有红色数字（表示脚本在该页面运行中）

### Q: 粘贴截图没反应？

**A:** 确认使用的是 `Win + Shift + S`（Windows 自带截图），然后回到面板区域按 `Ctrl + V`。不要用 QQ/微信的截图工具（它们不会把图片复制到系统剪贴板）。

### Q: 答案不准确？

**A:** 尝试切换到「🧠 深度思考」或「🔥 极限推理」模式，让 AI 更仔细地思考。

### Q: 自动检测找不到题目？

**A:** 自动检测抓取的是页面上的可见文字。如果题目是图片形式，请使用截图模式。

### Q: 「填入页面」没效果？

**A:** 先把光标点到要填入的输入框里，再点击「✏️ 填入页面」。

### Q: 怎么关闭服务？

**A:** 关闭 `start.bat` 打开的黑色命令行窗口即可。

---

## 七、文件清单

```
QSanswer/
├── start.bat              ← 双击启动
├── server.py              ← Flask 服务
├── deepseek_client.py     ← DeepSeek API 封装
├── tampermonkey_script.js ← 浏览器用户脚本
├── requirements.txt       ← Python 依赖
├── 使用教程.md             ← 本文件
└── .env                   ← (自建) API Key 配置
```
